[package]
name = "my-panproto-app"
version = "0.1.0"
edition = "2024"

[dependencies]
panproto-core = "0.29.0"
# Optional features:
# panproto-core = { version = "0.29.0", features = ["full-parse", "git", "tree-sitter"] }
